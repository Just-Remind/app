import { User, Highlight, Book, CronJob } from "./apiTypes";
import { Card } from "./frontendTypes";

export type {
  // API TYPES
  User,
  Highlight,
  Book,
  CronJob,
  // FRONTEND TYPES
  Card,
};
